export class Persona {
    // Constructor class expressión
    constructor(public nombre: string, public apellidos: string, public edad: number) {
        // These are properties is property binding of objects        
    }
}
